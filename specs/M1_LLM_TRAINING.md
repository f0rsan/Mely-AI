# M1 LLM 训练 — W4–8

> 目标: 本地 LLM 微调（Unsloth QLoRA），私有角色模型管理，角色对话界面
>
> **阶段一（W4–6）**：微调核心 M1-A 到 M1-D（先发布）
> **阶段二（W7–8）**：角色对话界面 M1-E（随后发布）

## 前置条件
- M0 全部完成
- Ollama 可安装（用户机器有网络）
- RTX 3070 8GB 开发机
- Python 环境含 Unsloth 依赖

---

## 阶段一：微调核心（W4–6）

### W4: LLM 环境 + 数据集管理

#### M1-A — Ollama 环境集成 (2d)
- 检测 Ollama 是否已安装（本地 HTTP ping `localhost:11434`）
- 未安装 → 引导用户安装（应用内展示下载链接 + 步骤说明，不暴露终端）
- 通过按需下载框架拉取基础模型（Qwen2.5-7B Q4_K_M，~4.5GB）
- Ollama API 封装服务（列出模型、加载/卸载模型、生成推理）
- 健康检查接口 `GET /api/llm/health`
- 验收: 系统能检测 Ollama 状态，能通过 Ollama 运行 Qwen2.5-7B 推理一次

#### M1-B — 文字数据集管理 (3d)
- 支持两种输入格式（同一 UI 入口，自动识别）：
  - **人设文档**：Markdown / TXT（角色描述、性格、背景故事、口癖）
  - **对话样本**：JSONL / CSV（user/assistant 对话对）
- 自动格式转换 → 统一 ShareGPT 训练格式（`{"conversations": [{"from": "human", "value": "..."}, {"from": "gpt", "value": "..."}]}`）
- 数据集质量检测规则：
  - 条目数（建议最低 50 条对话；低于 20 条警告）
  - 格式合规性检查（必须有 human / assistant 交替）
  - 平均回复长度检查（过短回复警告）
- 改进建议文字（中文，用户可见）
- 数据集预览（前 10 条样本展示）
- 验收: 分别上传人设文档 + JSONL 对话样本 → 合并成训练集 → 质量报告显示

### W5: Unsloth QLoRA 训练管线

#### M1-C — Unsloth QLoRA 训练后端 (3d)
- FastAPI 封装 Unsloth 训练流程
- REST API: `POST /api/llm-training/start`（接收角色 ID + 训练模式）
- `GET /api/llm-training/{task_id}/status`（查询进度）
- `POST /api/llm-training/{task_id}/cancel`（中断训练）
- 三档训练预设：

  | 模式 | 数据量建议 | 训练步数 | RTX 3070 耗时 | VRAM 占用 |
  |------|-----------|---------|--------------|----------|
  | 轻量 | 50–200 条 | 200–400 步 | ~15 min | ~6GB |
  | 标准 | 200–800 条 | 400–800 步 | ~35 min | ~6.5GB |
  | 精细 | 800+ 条 | 800–1500 步 | ~70 min | ~7GB |

- 实时进度 WebSocket 推送字段：`{step, total_steps, loss, eta_seconds}`
- GPU 互斥锁：训练期间禁止其他 GPU 任务（LoRA 图像训练、图像生成）
- 中断续训：每 100 步保存 checkpoint，重启后可从断点继续
- 错误处理：
  - VRAM 溢出 → 「显存不足，请尝试轻量模式或关闭其他程序」
  - 数据格式错误 → 「数据集格式有误，请检查第 N 条」
  - 训练发散（loss NaN）→ 「训练遇到问题，建议降低学习率或检查数据集」
- 验收: 上传 200 条对话 → 标准模式训练 → LoRA adapter 文件（`.safetensors`）生成

#### M1-D — 私有模型管理 (2d)
- LoRA adapter 存储路径：`~/.mely/characters/{id}/lm_loras/v{n}/`
- 训练完成后自动执行：
  1. Unsloth 合并 LoRA → 导出完整模型（GGUF Q4_K_M 格式，~4.5GB）
  2. 生成 Ollama Modelfile（注入角色人设 system prompt，来自角色 DNA）
  3. 执行 `ollama create character_{id}_v{n}` 注册模型
- 模型版本管理：
  - 每个角色可有多个微调版本（v1, v2, ...）
  - 版本元数据快照：训练参数 + 训练时间 + 数据集条目数 + loss 曲线
  - 支持删除旧版本（释放磁盘）
- 模型与角色绑定写入 `character_lm_models` 表
- 验收: 训练完成 → 自动导出 → Ollama 注册 → 通过 `GET /api/llm/models` 能看到新模型

### W6: Buffer + 集成测试

#### 集成测试 (2–3d)
- 完整流程 A：创建角色 → 上传人设文档 → 训练 → 导出 → Ollama 加载 → API 推理
- 完整流程 B：创建角色 → 上传 JSONL 对话样本 → 训练 → 同上
- 混合流程：人设文档 + 对话样本合并 → 训练
- 边界情况：数据集格式错误 / 训练中断 / VRAM 不足 / Ollama 未安装
- 验收: 所有流程跑通，异常全部有用户友好中文提示

---

## 阶段二：角色对话界面（W7–8）

### M1-E — 角色对话界面 (4d)

**UI 结构**
- 左侧：角色选择列表（显示头像 + 名称 + 当前使用的模型版本）
- 中间：对话消息流（气泡布局，角色头像在左，用户头像在右）
- 右侧：模型选择面板（基础模型 vs 各版本微调模型切换）

**功能**
- 流式输出（streaming，逐 token 显示）
- 系统 Prompt 自动注入：从角色 DNA 表读取人设摘要，构建 system prompt
- 模型对比模式：可同时开启两栏，分别用基础模型和微调模型回复同一条消息
- 对话历史归档：存入 `character_chats` 表（角色 ID + 模型版本 + 消息列表）
- 清空对话按钮（不删除历史，创建新会话）
- 导出对话记录（TXT 格式）

**错误处理**
- Ollama 未运行 → 「正在启动语言引擎，请稍候…」→ 自动尝试启动
- 模型未注册 → 「角色模型尚未完成训练，请先进行微调」
- 推理超时 → 「回复超时，请重试」

- 验收: 选择微调角色 → 发送消息 → 流式输出 → 历史可查看 → 基础/微调模型切换有明显差异

### 集成测试 (1d)
- 完整链路：数据集上传 → 训练 → 注册 → 打开对话 → 和角色聊天
- 对比体验：同一角色，切换基础/微调模型，感受人设差异
- 验收: 阶段一到阶段二的完整路径跑通

---

## 交付物

**阶段一（W4–6）**
- [ ] Ollama 环境集成 + 基础模型（Qwen2.5-7B）下载
- [ ] 文字数据集管理（人设文档 + 对话样本双格式，自动转换）
- [ ] Unsloth QLoRA 训练管线（三档预设 + 进度可视化 + 中断续训）
- [ ] Unsloth 合并导出 GGUF + Ollama 自动注册
- [ ] 私有模型版本管理

**阶段二（W7–8）**
- [ ] 角色对话 Chat UI（流式输出 + 角色头像）
- [ ] 基础/微调模型切换 + 对比模式
- [ ] 对话历史归档 + 导出
